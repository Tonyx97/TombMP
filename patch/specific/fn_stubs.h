#pragma once

#include <algorithm>
#include <math.h>

short m_sin(short angle, uint8_t shift = 0);
short m_cos(short angle, uint8_t shift = 0);
unsigned long m_atan2(long x1, long y1, long x2, long y2);